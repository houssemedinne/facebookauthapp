{
  "name": "render-omniroute",
  "version": "1.0.4",
  "private": true,
  "engines": {
    "node": ">=22.22.2 <23 || >=24.0.0 <27"
  },
  "scripts": {
    "start": "omniroute --port ${PORT:-80}"
  },
  "dependencies": {
    "omniroute": "3.8.50"
  }
}
