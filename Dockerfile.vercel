FROM node:22.22.2-bookworm-slim

WORKDIR /app

COPY package*.json ./
RUN npm install --omit=dev && test -x /app/node_modules/.bin/omniroute

ENV NODE_ENV=production

# Vercel injects PORT at runtime.
EXPOSE 80

CMD ["npm", "start"]
